# Kernalization, Kernel Tricks

### Implementation:
Generate datasets where Kmeans, PCA, SVM perform bad.
Study the effects of Kernelization and dimentionality reduction.

### Packages:
  1) sklearn
  2) matplotlib
  3) numpy
